/**
 * Decorative moon orb floating in the top-right of the sky section.
 * Purely visual — no props, no state, no interaction.
 */
function Moon() {
  return <div className="moon" aria-hidden="true" />;
}

export default Moon;
